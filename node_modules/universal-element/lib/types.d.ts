export interface Element {
    getScrollOffset: (string: any, Context?: any) => Promise<any[]>;
    getBoundingClientRect: (string: any, Context?: any) => Promise<any[]>;
}
export interface Context {
    createSelectorQuery: () => Query;
}
interface Query {
    selectAll: (string: any) => object;
}
export {};
