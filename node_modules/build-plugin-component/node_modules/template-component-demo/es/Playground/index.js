import React from 'react';
import { CodeBox } from './CodeBox';
import './index.css';
export var Playground = function Playground(_ref) {
  var data = _ref.data,
      children = _ref.children;
  var filename = data.filename,
      meta = data.meta,
      highlightedCode = data.highlightedCode,
      highlightedStyle = data.highlightedStyle,
      markdownContent = data.markdownContent,
      readme = data.readme; // add style scope

  var htmlContent = markdownContent || '';
  var matchCss = htmlContent.match(/<style>([\s\S]*?)<\/style>/);

  if (matchCss) {
    var matchContent = matchCss[1];
    var replaceContent = matchContent.split('}').map(function (content) {
      var cssContent = content;

      if (content[0] === '.') {
        cssContent = "#" + filename + " " + cssContent;
      }

      return "" + cssContent;
    }).join('}');
    htmlContent = htmlContent.replace(matchContent, replaceContent);
  }

  return /*#__PURE__*/React.createElement("div", null, renderMeta(meta), renderReadMe(readme), /*#__PURE__*/React.createElement("div", {
    className: "preview"
  }, renderMarkdownContent(htmlContent), /*#__PURE__*/React.createElement(CodeBox, {
    filename: filename,
    highlightedCode: highlightedCode,
    highlightedStyle: highlightedStyle
  }, children)));
};
/**
 * 渲染 README 文档
 */

function renderReadMe(readme) {
  if (readme === void 0) {
    readme = undefined;
  }

  if (!readme) return null;
  return renderMarkdownContent(readme);
}
/** 渲染 Meta 元素 */


function renderMeta(meta) {
  if (!meta) return null;
  return /*#__PURE__*/React.createElement("div", {
    className: "meta"
  }, /*#__PURE__*/React.createElement("ul", {
    className: "meta-list"
  }, meta.map(function (item) {
    return /*#__PURE__*/React.createElement("li", {
      className: "meta-item"
    }, item.key, ": ", item.value);
  })));
}
/** 渲染MarkDown内容 */


function renderMarkdownContent(markdownContent) {
  return /*#__PURE__*/React.createElement("div", {
    className: "markdown",
    dangerouslySetInnerHTML: {
      __html: markdownContent
    }
  });
}