import { CanvasContext, ContextAttributes } from '../types';
declare function createContext(selector: string, type?: string, options?: ContextAttributes): Promise<CanvasContext>;
export default createContext;
