"use strict";

exports.__esModule = true;
exports.toStyleText = void 0;

var toStyleText = function (styles) {
  var result = [];
  Object.keys(styles).forEach(function (k) {
    var val = styles[k];

    if (!val) {
      return;
    }

    var key = k.replace(/([A-Z])/g, function (it) {
      return "-" + it.toLowerCase();
    });
    result.push(key + ":" + val);
  });
  return result.join(';');
};

exports.toStyleText = toStyleText;