export declare type ModeType = 'week' | 'month' | 'year';
export declare enum CellStatusType {
    normal = "normal",
    disabled = "disabled",
    selected = "selected",
    current = "current",
    range = "range",
    begin = "begin",
    end = "end",
    today = "today"
}
export declare type ShapeType = 'card' | 'fullscreen' | 'panel' | 'range' | 'multi';
export declare type ContentType = 'fullscreen' | 'range' | 'normal' | 'multi';
export declare enum HeaderType {
    select = "select",
    normal = "normal"
}
export interface LocaleType {
    weekdays?: string[];
    months?: string[];
    year?: string;
    month?: string;
    day?: string;
    timePickerLabel?: string;
}
