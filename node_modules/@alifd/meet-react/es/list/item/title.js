function _extends() { _extends = Object.assign || function (target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i]; for (var key in source) { if (Object.prototype.hasOwnProperty.call(source, key)) { target[key] = source[key]; } } } return target; }; return _extends.apply(this, arguments); }

import { __rest } from "tslib";
import classNames from 'classnames';
import React, { createElement } from "react";
import { Text } from "@alifd/meet-react-component-one";
import View from '../../view';
import { isString } from '../../utils';

var ListItemLabel = function ListItemLabel(props) {
  var _props$prefix = props.prefix,
      prefix = _props$prefix === void 0 ? 'mt-' : _props$prefix,
      className = props.className,
      children = props.children,
      others = __rest(props, ["prefix", "className", "children"]);

  var Wrapper = isString(children) ? Text : View;
  return /*#__PURE__*/React.createElement(Wrapper, _extends({}, others, {
    className: classNames("".concat(prefix, "list-item-title"), className)
  }), children);
};

export default ListItemLabel;