import { ForwardRefExoticComponent } from 'react';
import Popup from './popup';
import { DialogProps, DialogQuickInstance, LocaleType } from './types';
import './styles/index.scss';
export interface DialogComponent extends ForwardRefExoticComponent<DialogProps> {
    Popup: typeof Popup;
    show: (props: DialogProps) => DialogQuickInstance;
    hide: () => void;
    alert: (props: DialogProps) => DialogQuickInstance;
    confirm: (props: DialogProps) => DialogQuickInstance;
}
declare const ExpandedDialog: DialogComponent;
export { DialogProps, LocaleType };
export default ExpandedDialog;
