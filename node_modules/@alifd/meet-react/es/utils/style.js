export var toStyleText = function toStyleText(styles) {
  var result = [];
  Object.keys(styles).forEach(function (k) {
    var val = styles[k];

    if (!val) {
      return;
    }

    var key = k.replace(/([A-Z])/g, function (it) {
      return "-".concat(it.toLowerCase());
    });
    result.push("".concat(key, ":").concat(val));
  });
  return result.join(';');
};