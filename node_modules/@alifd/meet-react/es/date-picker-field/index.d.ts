/// <reference types="react" />
/// <reference types="react" />
import { DatePickerProps } from '../date-picker';
import { FieldElement } from '../field';
export interface DatePickerFieldProps extends Omit<DatePickerProps, 'format'> {
    name: string;
    dateFormat?: string;
}
export default class DatePickerFiled extends FieldElement<DatePickerFieldProps> {
    static displayName: string;
    renderPreview(value: any): string;
    renderControl(): JSX.Element;
}
