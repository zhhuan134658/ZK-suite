import { Component } from 'react';
import * as PropTypes from 'prop-types';
import { SliderProps } from './types';
import './index.css';
declare class Slider extends Component<SliderProps | any, any> {
    static defaultProps: {
        horizontal: boolean;
        showsPagination: boolean;
        loop: boolean;
        autoPlay: boolean;
        autoPlayInterval: number;
        index: number;
        paginationStyle: {};
        initialVelocityThreshold: number;
        verticalThreshold: number;
        horizontalThreshold: number;
        vertical: boolean;
    };
    static propTypes: {
        onChange: PropTypes.Requireable<(...args: any[]) => any>;
        paginationStyle: PropTypes.Requireable<object>;
    };
    private index;
    private height;
    private width;
    private loopIdx;
    private offsetX;
    private isSwiping;
    private total;
    private swipeView;
    private childRefs;
    private isAutoPlay;
    private autoPlayTimer;
    constructor(props: SliderProps);
    componentDidMount(): void;
    componentDidUpdate(): void;
    componentWillUnmount(): void;
    private autoPlay;
    slideTo(index: number, direction: string): void;
    private onSwipeBegin;
    private isLoopEnd;
    private onSwipe;
    private onSwipeEnd;
    private loopedIndex;
    private renderPagination;
    private getPages;
    private renderSwipeView;
    render(): JSX.Element;
}
export default Slider;
