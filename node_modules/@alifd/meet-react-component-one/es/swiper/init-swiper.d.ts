export function initSwiper({ el, nextEl, prevEl, paginationEl, scrollbarEl }: {
    el: any;
    nextEl: any;
    prevEl: any;
    paginationEl: any;
    scrollbarEl: any;
}, swiperParams: any): Swiper;
import Swiper from "swiper";
