import { PartialPlugin, Options } from './types';
declare const genRuntime: ({ context }: PartialPlugin, { moduleExternals, modules, outputDir, filenameStrategy }: Options) => void;
export default genRuntime;
