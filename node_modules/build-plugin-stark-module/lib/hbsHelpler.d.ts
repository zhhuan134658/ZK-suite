interface compileParams {
    template: string;
    templatePath?: string;
    outputPath: string;
    params?: object;
}
export declare const compileTemplate: ({ template, templatePath, outputPath, params, }: compileParams) => void;
export {};
