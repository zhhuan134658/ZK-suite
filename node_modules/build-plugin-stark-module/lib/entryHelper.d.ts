import { Json } from './types';
export declare const getModules: (modules?: Json<string>[]) => Json<string>[] | {
    index: string;
};
export declare const hasModules: (modules?: Json<string>[]) => boolean;
