import { IPluginAPI } from 'build-scripts';
import { Options } from './types';
interface GetConfig {
    (api: Partial<IPluginAPI>, options?: Options): any;
}
declare const getConfig: GetConfig;
export default getConfig;
