"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var getExternals = function (externals) {
    var localExternals = __assign({}, externals);
    Object.keys(localExternals)
        .forEach(function (key) {
        var _a;
        if (typeof localExternals[key] !== 'string') {
            if (!localExternals[key].root) {
                throw new Error('[build-plugin-icestark-module] property root is missing in externals!');
            }
            if (!((_a = localExternals[key]) === null || _a === void 0 ? void 0 : _a.commonjs2)) {
                localExternals[key] = localExternals[key].root;
            }
        }
    });
    return localExternals;
};
var setExternals = function (_a, _b) {
    var onGetWebpackConfig = _a.onGetWebpackConfig;
    var moduleExternals = _b.moduleExternals;
    onGetWebpackConfig('icestark-module', function (config) {
        config.externals(getExternals(moduleExternals));
    });
};
exports.default = setExternals;
//# sourceMappingURL=setExternals.js.map