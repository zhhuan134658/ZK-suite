import { ProjectInfo } from '../index';
/**
 * 从gitlab、github、bucket导入
 * @param repository
 * @param destination
 */
export default function download<T>(repository: string, destination: string): Promise<T>;
/**
 * swap create clone & cp
 * @param projectInfo
 * @param templateType
 */
export declare function downloadTemplate(projectInfo: ProjectInfo, suiteDir: string, templateType: string): Promise<void>;
/**
 * swap debug 时 clone & cp
 */
export declare function downloadDebugTemplate(): Promise<void>;
