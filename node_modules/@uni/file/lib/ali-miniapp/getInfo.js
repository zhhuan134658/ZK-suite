"use strict";

exports.__esModule = true;
exports.default = void 0;

var _miniappEnvApp = require("../_utils/miniappEnvApp");

var _common = require("../common");

var _constant = require("../_utils/constant");

var getInfo = _common.normalize.getInfo(function (options) {
  var filePath = options.filePath,
      digestAlgorithm = options.digestAlgorithm,
      _success = options.success,
      _fail = options.fail,
      _complete = options.complete;
  var getFileInfo = _miniappEnvApp.isDingdingMiniapp ? dd.getFileInfo : my.getFileInfo;
  getFileInfo({
    apFilePath: filePath,
    digestAlgorithm: digestAlgorithm,
    success: function success(res) {
      _success && _success(res);
    },
    fail: function fail(res) {
      _fail && _fail(res);
    },
    complete: function complete(res) {
      _complete && _complete(res);
    }
  });
}, _constant.CONTAINER_NAME.ALIPAY);

var _default = getInfo;
exports.default = _default;