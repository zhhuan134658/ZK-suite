"use strict";

exports.__esModule = true;
exports.default = void 0;

var _constant = require("../_utils/constant");

var _common = require("../common");

var upload = _common.normalize.upload(function (options) {
  var url = options.url,
      filePath = options.filePath,
      fileName = options.fileName,
      hideLoading = options.hideLoading,
      header = options.header,
      formData = options.formData,
      _success = options.success,
      _fail = options.fail,
      _complete = options.complete;
  return tt.uploadFile({
    url: url,
    filePath: filePath,
    name: fileName,
    hideLoading: hideLoading,
    header: header,
    formData: formData,
    success: function success(res) {
      _success && _success(res);
    },
    fail: function fail(res) {
      _fail && _fail(res);
    },
    complete: function complete(res) {
      _complete && _complete(res);
    }
  });
}, _constant.CONTAINER_NAME.BYTE);

var _default = upload;
exports.default = _default;