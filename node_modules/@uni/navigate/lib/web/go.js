"use strict";

exports.__esModule = true;
exports.default = void 0;

var _common = require("../common");

var _constant = require("../_utils/constant");

var go = _common.normalize.go(function (options) {
  var step = options.step,
      success = options.success,
      fail = options.fail,
      complete = options.complete;

  if (step < 0) {
    setTimeout(function () {
      history.go(options.step);
      success && success();
      complete && complete();
    });
  } else {
    fail && fail({
      errMsg: 'step不能大于或等于0'
    });
    complete && complete({
      errMsg: 'step不能大于或等于0'
    });
  }
}, _constant.CONTAINER_NAME.WEB);

var _default = go;
exports.default = _default;